# =============================================================================
# Apexon AI Agent — Python Dependencies (Pinned)
# =============================================================================
# Install: pip install -r requirements.txt
#
# Version Resolution:
#   langgraph 0.2.76 → langgraph-checkpoint >=1.0.2,<2.0.0 → resolved to 2.1.2
#   langgraph-checkpoint-postgres 3.0.5 → langgraph-checkpoint >=2.0.0,<3.0.0
#   All versions verified to resolve without conflicts.
# =============================================================================

# ---------------------------------------------------------------------------
# Web Framework
# ---------------------------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6
sse-starlette==2.1.2

# ---------------------------------------------------------------------------
# Configuration & Validation
# ---------------------------------------------------------------------------
pydantic>=2.9.0
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ---------------------------------------------------------------------------
# Database (Async PostgreSQL)
# ---------------------------------------------------------------------------
psycopg[binary,pool]>=3.2.1

# ---------------------------------------------------------------------------
# AI / Agent Core
# ---------------------------------------------------------------------------
langgraph==1.1.3
langgraph-checkpoint-postgres==3.0.5
langchain-groq==1.1.2
langchain-core==1.2.22

# ---------------------------------------------------------------------------
# Static Analysis & Testing
# ---------------------------------------------------------------------------
ruff==0.6.8
mypy==1.11.2
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
