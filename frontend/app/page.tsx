/**
 * =============================================================================
 * Apexon AI Agent — Root Page
 * =============================================================================
 * Renders the main chat interface at the root route (/).
 * =============================================================================
 */

import ChatInterfaceFrontendComponent from "@/components/chatInterfaceFrontendComponent";

export default function HomePage() {
  return <ChatInterfaceFrontendComponent />;
}
